# el9 (Enterprise Linux 9) specific settings

# --- Eigen flags for el9 ---
CMS_EIGEN_CXX_FLAGS: "-DEIGEN_DONT_PARALLELIZE -DEIGEN_MAX_ALIGN_BYTES=64"

# --- LTO settings ---
lto_build_flags: "-flto=auto -fipa-icf -flto-odr-type-merging -fno-fat-lto-objects -Wodr"
